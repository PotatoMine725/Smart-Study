# Epic 2 / T2.4 — PR-6 `ConflictResolver`: engineering DoR and design review

| | |
|---|---|
| **Date** | 2026-09-11 |
| **Branch** | `docs/epic2-t2.4-pr6-dor-review` (isolated worktree `.claude/worktrees/pr6-dor-review`) |
| **Base** | `origin/dev@b198df6` (PR #78 merge) |
| **Kind** | Design review / Definition of Ready. **No production or test code was written.** |
| **Verdict** | **NOT READY** for implementation authorization — four owner decisions block (§15.1). In this folder's vocabulary that is `block` on *authorization*, not on the design: once §15.1 is answered, the design below is implementation-ready without further design work. |

**What the owner has to act on:** the four blocking items in §15.1. Everything in §15.2 is an
engineering choice PR-6 records and the owner may override. Everything in §15.3 is reported for
awareness, is not PR-6's to fix, and does **not** gate PR-6 starting once §15.1 is answered.

---

## 0. How to read this document

- This is a **delta over DoR §13 and §17 Z-1..Z-9** (`docs/review/2026-09-08-t2.3-t2.4-engineering-dor.md`).
  It does not restate §13; it cites it by number and spends its words on what §13 does not say,
  what PR-5 left behind, and what the frozen text makes impossible.
- Every important statement carries one label:
  - **FACT** — read directly from code or a tracked document at `origin/dev@b198df6`, with `file:line`.
  - **INFERENCE** — technical reasoning from facts; not measured in this pass.
  - **RECOMMENDATION** — a proposed engineering approach. Never a semantic decision.
- No semantic decision is made here. Where the frozen text leaves a real choice open, it is in §15.1
  as an owner decision with options, and the recommendation is marked as a recommendation.

### 0.1 Semantic authority read (in precedence order)

1. `docs/specs/T2.3-T2.4-D1-D9-Decision-Record-updated.md` — D6-H, D7-A..G, D8-A..H, D9 §14–§22,
   §23A D9-T1..T6. Not modified.
2. `docs/specs/T2.3-T2.4-D4-D9-T4-Amendment-2026-09-10.md` — absent local candidate for a StructuralConflict.
3. `docs/specs/T2.4-PR5-ContextLifetime-Amendment-2026-09-11.md` — Option A, **scoped to PR-5 only**.
4. `docs/review/2026-09-08-t2.3-t2.4-engineering-dor.md` — §8, §9, §11, §12, §13, §17, §19 and the
   2026-09-08 amendment (ACK A1, A2-a/b/c approved; §13.4 therefore frozen).
5. `docs/review/2026-09-09-t2.4-pr5-pr2-residuals.md` — R1, R2.
6. `docs/review/2026-09-10-t2.4-pr5-apply-session.md` — PR-5 as built; residual table §8 (row 12).

---

## 1. Scope reviewed and how the evidence was produced

**Code read at its final state on `origin/dev@b198df6`** (not from notes):

| File | Why |
|---|---|
| `SmartStudyPlanner/Sync/Apply/SyncApplySession.cs` | staging paths PR-6 must resolve; scope-lock; parent rules; cascade; `WriteAsync` (R1) |
| `SmartStudyPlanner/Sync/Apply/ConflictStaging.cs` | `ToRow` (what the record contains), M5, `SyncBaseFingerprint` |
| `SmartStudyPlanner/Sync/Apply/SyncApplyModels.cs`, `EntitySnapshotMapper.cs` | outcome/reason idiom; entity ⇄ snapshot; `Rev` handling |
| `SmartStudyPlanner/Sync/SyncConflictRecordStore.cs`, `SyncConflictRecordRow.cs` | `GetAsync`, `MarkResolvedAsync`, `FindUnresolvedByScopeKeyAsync`; row shape |
| `SmartStudyPlanner/Data/SyncConflictRecordSchema.cs`, `AppDbContext.cs`, `SyncStamper.cs` | triggers, CHECK, partial index; `MarkSyncApplied`; both `SaveChanges*` overloads |
| `SmartStudyPlanner/Sync/Merge/ConflictKeys.cs`, `MergeOutcomes.cs`, `ThreeWayMerge.cs`, `ConstraintMerge.cs`, `CanonicalJson.cs` | key/scope derivation; candidate liveness |
| `SmartStudyPlanner/Sync/SyncChangeEnumerator.cs` | emission predicate (`:30`) |
| `SmartStudyPlanner.Tests/Sync/Apply/SyncApplyConflictStagingTests.cs` (U), `SyncApplyParentHandlingTests.cs` (M) | what is already measured in-tree |

**Independently re-derived this pass:** the GitNexus impact numbers in §5 (fresh index, see §1.2);
every `file:line` citation. **Not re-derived:** the test-suite size. The 781-test baseline is read
from the PR-5 report (780) plus PR #77 (+1); the suite was **not** run in this session.

### 1.1 Baseline and workflow (FACT)

- `git fetch origin`; `dev` and `origin/dev` both at `b198df6`, **0 ahead / 0 behind**.
- The owner's checkout is **dirty with untracked files** (`.agents/`, 17 `.claude/skills/*` dirs,
  `skills-lock.json`, `docs/review/2026-09-08-t2.3-t2.4-current-code-design-notes.md`,
  `docs/review/2026-09-08-t2.3-t2.4-owner-triage.md`, `docs/specs/T2.3-T2.4-D1-D9-Decision-Record.md`).
  Reported, not touched.
- This review was produced in a worktree created from `origin/dev` (`git worktree add -b
  docs/epic2-t2.4-pr6-dor-review … origin/dev`). The owner's worktree was not modified. No
  destructive git command was used.
- **PR-5 residual #1 ("specs on no branch") is now closed**: `d6e668d` tracked the Decision Record,
  the DoR and the residuals note. The DoR housekeeping item to retire the stale
  `docs/specs/T2.3-T2.4-D1-D9-Decision-Record.md` is still open — that file now exists only as an
  untracked copy in the owner's tree.

### 1.2 GitNexus — what this pass did to the index (reported, not buried)

- The canonical `Smart-Study` entry is **stale at `723c5e2`** (34 commits behind; predates PR-1…PR-5),
  so it cannot answer impact questions about `Sync/*` — the same limitation PR-5 §9 recorded.
- To get real numbers this pass ran `npx gitnexus analyze --skip-agents-md --name
  Smart-Study-pr6-review .` **from the review worktree**. That registered an **additional**
  worktree-derived index entry (`Smart-Study-pr6-review`, 6,729 nodes / 17,437 edges at `b198df6`) —
  the same kind of registry pollution PR-5 declined to create. It was registered under a distinct
  alias so `Smart-Study` is not made ambiguous.
- Despite `--skip-agents-md`, the analyzer **rewrote six tracked files** in the review worktree
  (`.claude/skills/gitnexus/{gitnexus-cli,gitnexus-debugging,gitnexus-exploring,gitnexus-guide,gitnexus-impact-analysis,gitnexus-refactoring}/SKILL.md`).
  They are left **uncommitted** and are not part of this deliverable.
- **Follow-up for the owner:** prune the worktree-derived entries (`…pr4-conflict-record`,
  `Smart-Study-pr6-review`) and re-analyze the canonical path so impact analysis in PR-6 runs against
  `Smart-Study` itself.

---

## 2. What already exists for PR-6 to consume (FACT)

PR-4 and PR-5 shipped every persistence primitive DoR §13.2 needs; **none has a production caller yet**.

| Primitive | Where | Production callers today |
|---|---|---|
| `SyncConflictRecordStore.GetAsync(db, conflictId)` (tracked `FindAsync`) | `SyncConflictRecordStore.cs:49-53` | none |
| `SyncConflictRecordStore.MarkResolvedAsync(...)` — field-setter only, no save, no validation | `SyncConflictRecordStore.cs:137-154` (its doc, `:119-136`, names PR-6 as the owner of every check) | **none** (6 test callers, GitNexus MEDIUM) |
| `SyncConflictRecordStore.FindUnresolvedByScopeKeyAsync` | `:65-70` | `SyncApplySession` scope-lock only |
| `SyncBaseFingerprint.Matches(stored, currentLive)` — `stored ?? fp(null)` vs `fp(live)` | `ConflictStaging.cs:174-187` | **none** (1 test caller: U, GitNexus LOW) |
| `LocalRowRev` (Rev at staging) and `LocalWithdrawal` on the record | `ConflictStaging.cs:71-72`; `SyncConflictRecordRow.cs:73-75` | written by staging; read by nobody |
| Triggers: evidence immutable, Resolved terminal, no delete | `SyncConflictRecordSchema.cs:139-157` | DB-level backstop |
| Partial unique index: one Unresolved per `ScopeKey` | `SyncConflictRecordSchema.cs:135-137`; `AppDbContext.cs:174-176` | DB-level backstop |
| CHECK: local candidate columns present/absent together; absence only for `Kind = 1` | `SyncConflictRecordSchema.cs:71-74`; `AppDbContext.cs:167-171` | DB-level backstop |

**FACT — the only records a human can resolve are Structural (MonHoc, StudyTask) and Constraint
(TaskNote).** `ToRow` sets `Status = Unresolved` only when `AutoResolution` is null
(`ConflictStaging.cs:78`); Field and Tombstone evidence always carries `AutoLww`/`AutoTombstone`
(`ThreeWayMerge.cs:79-86,179-184`; `SyncApplySession.cs:768-774`); Structural candidates exist only
for the two D4 fields (`SyncApplySession.cs:786-791`); Constraint candidates only for the TaskNote
scope (`SyncApplySession.cs:291-337`). **The resolver's write surface is therefore exactly three
entity types: `MonHoc`, `StudyTask`, `TaskNote`.**

### 2.1 The five record shapes PR-6 must resolve (FACT, from the staging paths)

| Shape | Produced by | Base | Local | Remote | Live at staging | `LocalWithdrawal` | `LocalRowRev` |
|---|---|---|---|---|---|---|---|
| **S1-CR** ConcurrentReparent | `StageStructuralAsync` `:347-374` | present | present (live) | present (live) | row rewritten to whole Base row, **marked** (Base provenance kept) | `RewrittenToBase` | local Rev |
| **S1-PT** ParentTombstoned, local row exists | `StageParentTombstonedAsync` `:419-427` → S1 path | present | present | present (live; its parent is tombstoned) | same as S1-CR | `RewrittenToBase` | local Rev |
| **AL-PT** ParentTombstoned, no local row (D4/D9-T4 amendment) | `StageAbsentLocalStructuralAsync` `:452-468` | **null in every reachable case** (see note) | **absent** | present (live) | no row | `None` | null |
| **S2** Constraint, Base ≠ null | `StageConstraintAsync` `:507-513` | present (`N0`) | `N0'` (same Id as Base) | `N2` | `N0'` rewritten to `N0` | `RewrittenToBase` | local Rev |
| **S3** Constraint, Base = null | `StageConstraintAsync` `:492-505` | null | `N1` | `N2` (may be a **tombstone**, see §11.4) | **no row** — `N1` hard-deleted (M5) | `HardDeleted` | `N1.Rev` |

Note on AL-PT: `ApplyEntityAsync` rejects `LocalRowMissing` whenever a baseline exists but the local
row does not (`SyncApplySession.cs:175-181`), so the absent-local path is only ever reached with
`baseSnapshot == null`. **FACT.** S2 is unreachable in v1 by construction (DoR §9, S2 row); PR-5
implements it anyway and PR-6 must not crash on it.

---

## 3. PR-6 implementation boundary

### 3.1 In scope (the nine stated capabilities, nothing else)

1. `ConflictResolver` — `Sync/Apply/ConflictResolver.cs`, public entry `ResolveAsync(Guid conflictId,
   ResolutionRequest req, CancellationToken)` (DoR §13.1).
2. The four human kinds `KeepLocal` / `KeepRemote` / `KeepBase` / `ManualMerge` (D6-H).
3. Replay classification using `ConflictId` (addressing), `ConflictKey` / result identity (replay),
   `ScopeKey` (lock and drift scope) — §9.
4. The drift gate at **resolution-apply time** (D8-H, DoR §13.2) — §10. *Not* on the sync-apply path (§14).
5. Integration with the PR-5 seam: ordinary unmarked write through `SaveChangesAsync`/`SyncStamper`,
   `Rev` rules — §8.
6. Consumption of `SyncBaseFingerprint` and `LocalRowRev` — §10, §8.3.
7. Transaction/context lifetime and rollback rule R2 — §8.
8. R1 — §8.2.
9. Residual 12 — §11.1.

### 3.2 Affected files / types / methods

| Change | File | Type / member | Kind |
|---|---|---|---|
| **new** | `Sync/Apply/ConflictResolver.cs` | `ConflictResolver(Func<AppDbContext>)`, `ResolveAsync` | production |
| **new** | `Sync/Apply/ConflictResolutionModels.cs` (or inside the above) | `ResolutionRequest(ResolutionKind Kind, EntitySnapshot? ManualResult, Guid? ManualResultEntityId)`; `ResolutionOutcome { Applied, NoOpReplay, Rejected, Failed }`; `ResolutionRejectReason { InvalidRequest, UnknownConflict, NotResolvable, MismatchedReplay, LiveStateDrift, KindNotApplicable, ResultParentTombstoned, ResultParentMissing, ResultIdInUse, ResultOutOfScope, ScopeOccupied, ScopeHasUnresolvedConflict, ContractViolation }`; `ResolutionResult` | production |
| consumed, **unchanged** | `Sync/SyncConflictRecordStore.cs` | `GetAsync`, `MarkResolvedAsync`, `FindUnresolvedByScopeKeyAsync` | — |
| consumed, **unchanged** | `Sync/Apply/ConflictStaging.cs` | `SyncBaseFingerprint.Matches` / `Of` | — |
| consumed, **unchanged** | `Sync/Apply/EntitySnapshotMapper.cs` (internal, same assembly) | `ToSnapshot`, `ApplyTo`, `CreateEntity`, `IdOf` | — |
| consumed, **unchanged** | `Sync/Merge/CanonicalJson.cs`, `ConflictKeys.cs` | `Read`/`Write`/`Fingerprint`; `ScopeKey` | — |
| consumed, **unchanged** | `Data/AppDbContext.cs`, `Data/SyncStamper.cs` | `SaveChangesAsync` (ordinary path only) | — |
| **optional**, owner's call (§15.2 E-7) | `Sync/Apply/SyncApplySession.cs` | move the parent-rule table + `LoadParentAsync`/`LoadTrackedAsync`/`LoadNoTrackingAsync` into an internal helper, behaviour-preserving | refactor |
| **new tests** | `SmartStudyPlanner.Tests/Sync/Apply/ConflictResolver*Tests.cs`; reuse `Fixtures/SyncApplyFixture.cs`, `TestDoubles/FailingSaveDbContext.cs` | §13 | test |

**No schema change. No change to `SyncStamper`, `AppDbContext`, `SyncBaseSnapshotStore`,
`SyncConflictRecordStore`, the merge core, or any repository.** RECOMMENDATION.

---

## 4. Call graph

```text
caller (test harness in M2.1; no UI in v1 — §14)
  └─ ConflictResolver.ResolveAsync(conflictId, req)
       db = factory()                                   ← one context per call (§8.1, blocked on B-1)
       tx = db.Database.BeginTransactionAsync()
         SyncConflictRecordStore.GetAsync(db, id)        ← tracked record
         [classify: request → auto → replay → applicability]     (§7.2)
         load live scope rows, AsNoTracking               ← EntitySnapshotMapper.ToSnapshot
         SyncBaseFingerprint.Matches(rec.BaseFingerprint, live)   ← D8-H drift gate
         derive result (rec.Local/Remote/Base JSON → CanonicalJson.Read | req.ManualResult)
         result checks: parent (§11), scope occupancy, scope-lock (this record excluded), id rules
         write result on the TRACKED instance, UNMARKED   ← EntitySnapshotMapper.ApplyTo / CreateEntity
         SyncConflictRecordStore.MarkResolvedAsync(...)   ← record → Resolved (tracked)
         db.SaveChangesAsync()                            ← ONE save: SyncStamper ordinary path stamps
                                                            the domain row (A2-c); record row is not
                                                            ISyncMetadata, stamper skips it
       tx.Commit  |  Rollback on reject/exception
       db.DisposeAsync()  (every path)
```

---

## 5. Blast radius (GitNexus, fresh index `Smart-Study-pr6-review` @ `b198df6`, upstream)

| Symbol | Risk | Impacted | Direct | PR-6 relationship |
|---|---|---|---|---|
| `SyncStamper.Apply` (4-arg, `#4`) | **CRITICAL** | 46 | 1 (`Apply#3`) | called indirectly, **not modified** |
| `AppDbContext.SaveChangesAsync` | **CRITICAL** | 44 | 35 | called, **not modified** |
| `AppDbContext.MarkSyncApplied` | **CRITICAL** | 24 | 17 (15 tests + `WriteAsync` + `TombstoneCascadeChildAsync`) | **must not be called** by PR-6 (§8.2) |
| `SyncApplySession.ApplyAsync` | **CRITICAL** | 40 | 40 (all tests) | not called, not modified |
| `SyncConflictRecordStore.StageAsync` | **CRITICAL** | 18 | 12 | not called |
| `EntitySnapshotMapper.ApplyTo` | **CRITICAL** | 10 | 3 | called, not modified |
| `SyncApplySession.InspectParentAsync` | HIGH | 4 | 1 (`ApplyEntityAsync`) | only touched if E-7 extraction is chosen |
| `SyncApplySession.CascadeTombstoneAsync` | HIGH | 4 | 1 | not touched (finding §11.3 is reported, not fixed) |
| `SyncConflictRecordStore.MarkResolvedAsync` | MEDIUM | 6 | 6 (all tests) | **first production caller** |
| `SyncBaseFingerprint.Matches` | LOW | 1 | 1 (test U) | **first production caller** |

**INFERENCE.** PR-6 as scoped is additive: it *calls* the CRITICAL seams and modifies none of them.
Its own blast radius is the new file plus the two currently caller-less primitives. The risk labels
are the graph's, dominated by test fan-in; counts are non-deterministic at the ~0.1% level (project
memory) and the numbers above are from a worktree index, not the canonical one — re-record them on
the canonical index before implementing (§15.3 W-5).

**Required before implementation / commit** (RECOMMENDATION, from DoR §18): `impact` upstream on
`MarkResolvedAsync`, `Matches`, `GetAsync`, `SaveChangesAsync`, and on every symbol moved if E-7 is
chosen; `detect_changes` before commit must list only the new resolver files and new tests (plus the
extraction commit's moved symbols, if any).

---

## 6. Invariants PR-6 must preserve

| # | Invariant | Source | Enforced by |
|---|---|---|---|
| I-1 | Unresolved → Resolved only; never back; Resolved terminal | D7-D, D9 §16 | trigger `ResolvedIsTerminal` (`SyncConflictRecordSchema.cs:148-152`) + code |
| I-2 | Base/Local/Remote evidence (24 columns incl. `LocalRowRev`, `LocalWithdrawal`) never mutated | D7-F | trigger `EvidenceImmutable` (`:139-146`) + code writes only via `MarkResolvedAsync` |
| I-3 | Auto records (`AutoLww`/`AutoTombstone`) are not re-resolvable | D9-T5 | code (§7.2 step 2) |
| I-4 | Domain write + `Status = Resolved` atomic | D8-E | one transaction, **one** `SaveChangesAsync` |
| I-5 | Resolution only when live scope state still equals Base; no force, no rebase | D8-H, D9 §17 | `SyncBaseFingerprint.Matches` |
| I-6 | Replay of the same resolution is a semantic no-op; a different one is rejected | D7-C, DoR §8.4 | code (§9.1) |
| I-7 | Result is written as an ordinary local write, stamped by `SyncStamper` | ACK A2-c (DoR §13.4, frozen) | no `MarkSyncApplied` (§8.2) |
| I-8 | `Rev` local-only; never copied from a snapshot; +1 exactly once per written row | D9 §21, DoR §11.1 | ordinary stamper path; seeding rule §8.3 |
| I-9 | No live child under a tombstoned structural parent; no live FK-only child under a tombstoned task | D9-T4, D4/D9-T4 amendment, ACK A2-a | result parent check (§11) |
| I-10 | At most one Unresolved record per scope; resolving frees the scope | D9-T6 | partial unique index |
| I-11 | No exception-driven conflict/constraint handling; UNIQUE is a backstop, not a detector | D9 §19 | typed pre-checks (§7.3) |
| I-12 | Exactly one physical-delete site in production (M5) | ACK A1, DoR §9.2 | `SyncApplyAuditFenceTests` — PR-6 adds **no** delete |
| I-13 | The resolution baseline is **not** advanced | DoR §13.5 | PR-6 never calls `SyncBaseSnapshotStore` |
| I-14 | A failed operation's context is never reused | R2 | per-call context (B-1) |

---

## 7. Conflict / resolution state transitions

### 7.1 Record lifecycle (FACT + frozen)

```text
                 staging (PR-5)                                resolution (PR-6)
candidate ──┬─ AutoResolution != null ──► Resolved(AutoLww|AutoTombstone) ── any request ──► Rejected(NotResolvable)
            └─ AutoResolution == null ──► Unresolved ──┬─ drift ─────────────────────────► Rejected(LiveStateDrift), stays Unresolved
                                                        ├─ result check fails ─────────────► Rejected(<reason>), stays Unresolved
                                                        └─ KeepLocal|KeepRemote|KeepBase|ManualMerge ──► Resolved
Resolved(human) ── same (Kind, ResultEntityId, ResultFingerprint) ──► NoOpReplay (writes nothing)
                └─ anything else ────────────────────────────────────► Rejected(MismatchedReplay)
```

### 7.2 Classification order inside one call (RECOMMENDATION; the order is load-bearing)

| Step | Check | Outcome on failure | Why here |
|---|---|---|---|
| 0 | request shape: `Kind ∈ {KeepLocal, KeepRemote, KeepBase, ManualMerge}`; ManualMerge carries result + id; others carry neither | `Rejected(InvalidRequest)` | `ResolutionKind` includes the auto kinds (`MergeOutcomes.cs:19`); a caller must not be able to request one |
| 1 | load by `ConflictId` | `Rejected(UnknownConflict)` | D7-A: `ConflictId` is the addressing identity |
| 2 | record is auto (`ResolutionKind ∈ {AutoLww, AutoTombstone}`) | `Rejected(NotResolvable)` | **before** replay: D9-T5 forbids re-resolution even for a request that "matches" |
| 3 | record `Resolved` → compare request identity | `NoOpReplay` / `Rejected(MismatchedReplay)` | **before** drift: a legitimate replay after the row has since moved on must still be `NoOpReplay`, not `LiveStateDrift` (tested, §13 Z-R1) |
| 4 | Unresolved record has `Kind ∈ {Structural, Constraint}` | `Rejected(ContractViolation)` | §2: nothing else can be Unresolved |
| 5 | kind applicable to shape (KeepLocal on AL-PT — **B-4**) | `Rejected(KindNotApplicable)` | |
| 6 | drift gate (§10) | `Rejected(LiveStateDrift)` | D8-H |
| 7 | derive + validate result (ManualMerge: §5.5 canonical round-trip, `EntityType`, constraint `MaTask == ConstraintValue`, id rules §13.3/E-3) | `Rejected(InvalidRequest | ResultOutOfScope | ResultIdInUse)` | |
| 8 | result checks: parent (§11, **B-2**), scope occupancy (§7.3), scope-lock on the result's scope with this record excluded | `Rejected(ResultParent* | ScopeOccupied | ScopeHasUnresolvedConflict)` | D9-T4, D9 §19, D9-T6 |
| 9 | write (unmarked) + `MarkResolvedAsync` + one save; commit | `Failed` on exception (rollback, dispose) | D8-E |

### 7.3 Shape × kind matrix (FACT for mechanics, INFERENCE for reachability, **bold** = needs §15.1)

| Shape | KeepLocal | KeepRemote | KeepBase | ManualMerge |
|---|---|---|---|---|
| S1-CR | update row to Local; Rev+1; local stamp | update row to Remote; Rev+1; local stamp | result == live (drift gate guarantees it) ⇒ no field changes ⇒ EF leaves the entry `Unchanged` ⇒ **no domain write, no Rev++** (E-5) | update row to D; `ResultEntityId` = `EntityId` (E-3) |
| S1-PT | as S1-CR, subject to result parent check (**B-2**) | Remote's parent is tombstoned ⇒ **must not write** (Residual 12, **B-2** outcome) | as S1-CR; in the M-test shape Base's own parent is tombstoned too (**B-2** sub-question) | allowed only if D's parent is live (**B-2**) |
| AL-PT | **B-4** (no local candidate exists) | Remote's parent is tombstoned ⇒ **must not write** (Residual 12) | Base null ⇒ nothing to write; `ResultSnapshotJson = null` (D7-G) | reparent to a live parent ⇒ create; `ResultEntityId` = `EntityId` (E-3) |
| S2 (unreachable) | `N0` live in scope; Local has Base's Id ⇒ update to `N0'` | `N2` ≠ `N0`'s Id and `N0` still occupies UNIQUE(MaTask) ⇒ `Rejected(ScopeOccupied)` — typed, never a second delete site (E-8) | no write (as S1-CR) | same Id as Base ⇒ update; different Id ⇒ `ScopeOccupied` |
| S3 | re-insert `N1`, `Rev` seeded from `LocalRowRev` (E-2) | insert `N2` (Rev 1); `N2` may be a tombstone (§11.4) | Base null ⇒ nothing to write | insert with id ∈ {`N1` (seed Rev, E-2), `N2`, fresh}; `MaTask` = `ConstraintValue` |

---

## 8. Transaction, context, R1, R2, Rev

### 8.1 Context lifetime — **blocking (B-1)**

- **FACT.** DoR §13.1 says "one transaction per call" and is silent on context ownership.
- **FACT.** The Option A amendment is explicitly PR-5-only: *"It does not apply to any component
  outside PR-5's sync-apply session"* (`T2.4-PR5-ContextLifetime-Amendment-2026-09-11.md`, §"Relationship
  to DoR §12.1"). So PR-6 has no ratified context lifetime.
- **INFERENCE — the resolver's R2 exposure is narrower than PR-5's, but not zero.** The resolver never
  marks, so `SyncStamper` cannot throw on its entries (the throw is only on the marked path,
  `SyncStamper.cs:80-94`). The remaining shape is: the ordinary path has already done `Rev++` and
  written `ModifiedAtUtc`/`ModifiedByDeviceId` in memory (`SyncStamper.cs:54-59`), then
  `base.SaveChangesAsync` fails (trigger ABORT on a racing resolve, UNIQUE(MaTask), CHECK, FK). The
  context is left holding a stamped, dirty instance ahead of the database — R2 Case A's state, reached
  by a different trigger. The resolver has one save, so R2 Case B (two saves) does not arise.
- **RECOMMENDATION.** Extend Option A to PR-6: `ConflictResolver` takes the composition-root
  `Func<AppDbContext>`, creates one context per `ResolveAsync` call, disposes it on every path, never
  reuses it for a retry; a retry is a new call that re-reads the record (and may then correctly
  classify as `NoOpReplay` if the earlier attempt actually committed).

### 8.2 R1 and the seam — the resolver does not mark

- **FACT.** ACK A2-c (DoR §13.4, frozen by the 2026-09-08 amendment): the result is written as an
  ordinary local write, `SyncStamper` stamps `ModifiedAtUtc = now`, `ModifiedByDeviceId = this device`,
  `Rev++`. `SyncApplySession.WriteAsync` marks unconditionally (`SyncApplySession.cs:566`) and is
  therefore **not reusable** by the resolver.
- **RECOMMENDATION.** R1 becomes a *negative* invariant for PR-6: **zero** `MarkSyncApplied` calls in the
  resolver. The resolver keeps R1's *positive* half anyway — it writes onto the tracked instance
  (`LoadTrackedAsync`-style raw-DbSet query), never onto the `AsNoTracking` drift snapshot, and never
  via `DbSet.Update(detached)` (the PR-A defect shape). The write helper should assert
  `Entry(entity).State != Detached` before save for the same reason PR-5's does.
- **INFERENCE.** Provenance on the written row: `ApplyTo` copies the snapshot's provenance block
  (`EntitySnapshotMapper.cs:220-226`), then the ordinary stamper overwrites `ModifiedAtUtc` and
  `ModifiedByDeviceId` but **not** `IsDeleted`/`DeletedAtUtc` (`SyncStamper.cs:54-59`). A tombstone
  result therefore keeps the candidate's `DeletedAtUtc` and gets a local `ModifiedAtUtc` — valid
  provenance (§11.4).

### 8.3 Rev (engineering; D9 §21 delegates it)

| Write | Rev after | Label |
|---|---|---|
| update of an existing row (S1-CR, S1-PT, S2 same-Id) | `Rev_live + 1` (ordinary Modified path) | FACT |
| KeepBase with Base present | unchanged (entry stays `Unchanged`; stamper visits only Added/Modified, `SyncStamper.cs:54`) | FACT for the stamper; INFERENCE for EF's no-change detection (the same property PR-5 residual 8 relies on) |
| S3 re-insert of `LocalEntityId` (KeepLocal, or ManualMerge choosing `N1`) | seed `Rev = LocalRowRev` before `Add`; stamper's Added path ⇒ `LocalRowRev + 1` | RECOMMENDATION (E-2) |
| insert of an Id new to this device (S3 KeepRemote `N2`, fresh ManualMerge id, AL-PT ManualMerge) | 1 | FACT |

**Why E-2 covers ManualMerge-with-`N1`, not only KeepLocal (INFERENCE).** DoR §13.2 seeds only for
KeepLocal. But `SyncBaseSnapshots` rows for `N1` held for *other* peers survive the M5 delete (DoR §9.1
M5 row). `SyncChangeEnumerator` emits `row.Rev > snapshot.Rev` (`SyncChangeEnumerator.cs:30`). Any
re-insert of `N1` at `Rev = 1` would sit at or below those baselines and never be emitted to those
peers. The rule is about the *identity* being re-materialised, not the resolution kind.

### 8.4 Rollback rule R2 applied

On any exception: roll back (explicitly, and by `await using` disposal — PR-5 mutation #14 measured that
disposal alone is sufficient), dispose, return `Failed`. On a typed rejection: roll back, dispose,
return `Rejected`. The record stays `Unresolved`; nothing partial is visible because the domain write
and the record update share one save (I-4).

---

## 9. Replay behaviour

### 9.1 Resolution replay (the resolver's own idempotency)

- **FACT.** DoR §8.4: identity of a resolution = `(ResolutionKind, ResultFingerprint, ResultEntityId)`.
- **INFERENCE — the stored result must be the requested/selected result, pre-stamp.** Under A2-c the
  persisted row carries `Clock()` provenance. If `ResultSnapshotJson`/`ResultFingerprint` were the
  post-stamp row, a replayed request could never recompute them, and every replay would classify as
  `MismatchedReplay` — violating D7-C. **RECOMMENDATION (E-1):** store `canonical(selected result)`
  exactly as chosen (KeepLocal ⇒ `rec.LocalSnapshotJson`, KeepRemote ⇒ `rec.RemoteSnapshotJson`,
  KeepBase ⇒ `rec.BaseSnapshotJson` or null, ManualMerge ⇒ `canonical(req.ManualResult)`), and state in
  the code that the live row's provenance intentionally differs (A2-c).
- `ResultEntityId`: KeepLocal ⇒ `LocalEntityId`; KeepRemote ⇒ `RemoteEntityId`; KeepBase ⇒
  `BaseEntityId` (null when Base is null); ManualMerge ⇒ request id.
- `NoOpReplay` writes nothing: no domain change, no `ResolvedAtUtc` update (the terminal trigger would
  abort one anyway).

### 9.2 Roles of the three identities in PR-6

| Identity | PR-6 use | Not used for |
|---|---|---|
| `ConflictId` | addressing the record (`GetAsync`) | replay (D7-A: never interchanged) |
| `ScopeKey` | drift-gate scope; scope-lock check on the result with this record excluded; freed on `Resolved` (partial index) | identity of the resolution |
| `ConflictKey` | **not read by the resolver.** It matters *after* resolution, on the sync path (§9.3) | resolution replay |

### 9.3 Sync replay after a resolution (FACT from code paths; INFERENCE where noted)

- **Same candidate re-offered after resolution collapses on `ConflictKey`.** Example AL-PT resolved
  with KeepBase(null): the same remote create arrives again ⇒ no local row, no lock (record now
  Resolved) ⇒ parent still tombstoned ⇒ `StageAbsentLocalStructuralAsync` ⇒ `StageAsync` finds the same
  `ConflictKey` (any status) ⇒ `ConflictAlreadyStaged`; nothing materialised
  (`SyncConflictRecordStore.cs:105-108`; `SyncApplySession.cs:459-463`).
- **Edited candidate after resolution creates a new Unresolved record** in the same scope (allowed:
  DoR §8.4 row 3, prior record Resolved).
- **Durability of a resolution against the peer's still-live state (INFERENCE, §15.3 W-3).** In the
  common asymmetric case the device that receives first stages, holds live at Base, and writes the
  peer baseline as `(Rev_post, canonical(Base))` (`SyncApplySession.cs:368-371`), so it never emits its
  withdrawn local candidate to the peer. The peer keeps its competing state live and keeps offering it
  (its `row.Rev` exceeds its baseline for us). After **KeepBase**, the lock is gone and the next pull
  applies the peer's state as an ordinary one-sided change — the human KeepBase is overridden on the
  same device. After **KeepLocal**, the locally stamped row is emitted to the peer, which stages its own
  conflict (DoR §13.6). After **KeepRemote**, both converge. This is a sharper, single-device statement
  of DoR §13.6 / OPEN-2; PR-6 must not solve it (no propagation, no baseline advance — §14).

---

## 10. Apply-time drift behaviour (resolution-time gate)

### 10.1 What "current live state of the scope" means (RECOMMENDATION, E-4)

| Shape | Rows fingerprinted | Expected |
|---|---|---|
| S1-CR, S1-PT | the row with `Id = EntityId`, raw DbSet, `AsNoTracking`, **tombstones included** | `BaseFingerprint` |
| AL-PT | same query | none (`BaseFingerprint` null ⇒ `fp(null)`) |
| S2 | the row with `MaTask = ConstraintValue` (UNIQUE ⇒ ≤ 1), tombstones included | `BaseFingerprint` |
| S3 | same query | none |

Tombstones are included because a tombstone is observable state and occupies UNIQUE(MaTask) (unfiltered
index, `SyncApplySession.cs:303-306`). The fingerprint must be produced by the same functions staging
used — `EntitySnapshotMapper.ToSnapshot` then `CanonicalJson.Fingerprint` — or every resolution will be
rejected as drift.

**FACT — the positive leg is partly measured in-tree.** `U_StoredBaseFingerprint_DetectsLiveStateDrift`
asserts `Matches(record.BaseFingerprint, snapshot(live))` is **true** right after S1-CR staging
(`SyncApplyConflictStagingTests.cs:582-583`), **false** after an ordinary local edit (`:585-594`), and
that `Matches(null, null)` is true (`:597`). Not yet measured: S1-PT, S2, S3/AL-PT through the real
query.

### 10.2 There is no drift gate on the sync-apply path — and PR-6 must not add one

**FACT.** `ConflictStaging.cs:168-172`: *"There is NO apply-time drift gate. On the apply path Base comes
from the baseline row and Local from the live row, and those are supposed to differ … Adding a gate here
would be inventing policy the frozen record does not contain."* The brief's "apply-time drift gate" is
read here as the gate at the time a **resolution** is applied (DoR §13.2), which is the only drift gate
the frozen record contains.

### 10.3 Finding F-1 — a drifted record can never be resolved (High; §15.3 W-1)

The legs, labelled separately:

| Leg | Evidence |
|---|---|
| An ordinary local edit to a row held at Base changes its fingerprint | **measured**, `SyncApplyConflictStagingTests.cs:585-594` |
| D8-H rejects **every** kind on drift, KeepBase included; no force, no rebase | **frozen text** (D8-H, D9 §17) |
| No reopen (D7-D), no supersede (D9-T6: "Supersede/reopen semantics are out of scope for v1") | **frozen text** |
| While Unresolved, every inbound sync op in that scope is rejected | **FACT**, `SyncApplySession.cs:205-212`; `StageAsync` `:110-113` |
| Nothing outside `Sync/`, `Data/` schema setup and `AppDbContext` references `SyncConflictRecords`, so local UI edits do not consult the lock | **FACT** (grep over `SmartStudyPlanner/`: 7 files, none a repository or view-model) |

Conclusion (inference only in composition): once a local edit touches a row held at Base (S1/S2), or a
note is created in an S3 scope, the record is permanently `Unresolved` and the scope is permanently
locked for inbound sync. In M2.1 (harness only, §15 of the DoR) this cannot happen in practice; in M2.2
it is an everyday UI action. **PR-6 must implement D8-H faithfully and must not answer this by inventing
force, rebase, abandon or reopen.** It should pin the behaviour with a characterization test (§13 Z-C1)
so a future owner decision has a measured baseline.

---

## 11. Parent tombstone and cascade-tombstone behaviour

### 11.1 Residual 12, generalised

- **FACT.** PR-5 §8 row 12: *"PR-6 must not execute `KeepRemote` on a `ParentTombstoned` record without
  first resolving the parent."*
- **FACT.** In v1 a tombstoned parent can never become live again: no resurrection path exists (DoR §19),
  and the merge core fails closed on a live side over a dead Base (`ThreeWayMerge.cs:45-53`).
- **INFERENCE.** "Until the parent is resolved" therefore means "never" in v1: KeepRemote on an AL-PT or
  S1-PT record whose Remote points at a still-tombstoned parent is **permanently** invalid, not deferred.
- **INFERENCE — Residual 12 is one instance of a general rule.** DoR §13.2 already requires a "parent
  check (§12.2) … on the result". Any result — KeepLocal, KeepRemote, KeepBase, ManualMerge — whose
  structural parent (MonHoc.MaHocKy, StudyTask.MaMonHoc) or FK parent (TaskNote.MaTask) is tombstoned or
  missing *at resolution time* would create what D9-T4 / A2-a forbid. The parent may also have been
  tombstoned **after** staging (e.g. S3 note, then its task is tombstoned by a later sync: the scope is
  empty, so the drift gate passes, and KeepLocal/KeepRemote would insert a live note under a dead task).
- **FACT — the gap that makes this blocking.** §12.2's outcomes are apply-time actions (stage a
  StructuralConflict; cascade-tombstone the child). At resolution time the record *is* the conflict, so
  "stage a StructuralConflict" is incoherent and "cascade-tombstone" silently changes the requested
  result. The typed outcome is unspecified → **B-2**.
- **FACT — a shape where no non-manual kind passes a strict check.** Test M's S1-PT shape
  (`SyncApplyParentHandlingTests.cs:112-141`) tombstones the parent locally without reaching the child
  (the documented Epic-1 gap), so Base, Local and Remote all point at the same tombstoned MonHoc, and the
  row held live at Base is *already* a live child under a tombstoned parent. Under a strict parent check
  on every result, only a ManualMerge that reparents can resolve it. Whether KeepBase — which writes
  nothing and creates no new orphan — is exempt is the B-2 sub-question.

### 11.2 Missing parent at resolution time

No deferral exists (the resolver is not a run). **RECOMMENDATION:** `Rejected(ResultParentMissing)`,
nothing written. Only reachable through ManualMerge (a candidate's parent cannot vanish without a hard
delete, which does not exist outside M5).

### 11.3 Finding F-2 — cascade tombstones write into locked scopes (Medium; §15.3 W-2)

- **FACT.** `CascadeTombstoneAsync` selects live children by FK and tombstones them via the marked path
  with no scope-lock lookup (`SyncApplySession.cs:589-645`); the lock is checked only for the operation's
  own entity scope (`:205-212`, key from `StructuralScopeKeyOf` `:798-801`).
- **INFERENCE (not measured).** A remote tombstone of the Base parent of a StudyTask held at Base in an
  S1 record cascades onto that task while its record is Unresolved ⇒ live ≠ Base (D9-T1 no longer holds)
  ⇒ at resolution every kind is rejected as drift ⇒ same end state as F-1. Two frozen rules collide
  here: D9-T1 ("live = Base while Unresolved") and D9-T4 / the Epic-1 cascade ("no live child under a
  dead parent"). Which wins is an owner question; PR-6's job is only to reject cleanly on the result.
- **Reproduction recipe** (for PR-6 as characterization Z-C2, or a separate follow-up), following the
  precedent of the PR-2 residuals note, which shipped labelled inference plus a recipe when its boundary
  forbade measurement and PR-5 then measured it: seed HocKy → MonHoc `M` → StudyTask `T`, baseline for
  the peer; local reparent `T → B`, remote reparent `T → C` ⇒ S1-CR staged, `T` held at `MaMonHoc = M`;
  then apply a remote tombstone of `M` (phase 1). Expect: `T.IsDeleted == true`, record still
  `Unresolved`, `Matches(record.BaseFingerprint, snapshot(T)) == false`. **Not written here because this
  task forbids test code.**
- PR-6 must **not** change the cascade (§14).

### 11.4 Tombstone results

- **FACT.** Structural candidates are always live (tombstones are resolved first by tombstone-wins,
  `ThreeWayMerge.cs:37-90`), and the AL-PT/S1-PT remote result is staged only when live
  (`SyncApplySession.cs:217`).
- **FACT.** `ConstraintMerge` tests only the **local** side for a tombstone (`ConstraintMerge.cs:41-45`),
  so an S3 record can carry a tombstoned `Remote` (peer created then deleted `N2`). KeepRemote then
  materialises a tombstone `N2` — valid provenance (§8.2), no cascade needed (TaskNote has no children).
  Test it; no owner decision needed (D6-H: Result = Remote).
- **INFERENCE → blocking (B-3).** The only way to produce a tombstone for a parent-owning type
  (MonHoc, StudyTask) is a ManualMerge result with `IsDeleted = true`. D6-H allows "a brand-new state";
  a tombstone is a state. Honouring it requires the Epic-1 cascade *and* A2-b's rule that cascade
  children copy the parent's tombstone provenance — but under A2-c the parent's provenance is stamped
  by `SyncStamper` at save time, so the children's values are not known before the save. DoR §13 never
  contemplates this.

---

## 12. Integration with PR-1 … PR-5

| Slice | What PR-6 relies on | Constraint on PR-6 |
|---|---|---|
| PR-A (Rev/CreatedAtUtc fix) | tracked-row update pattern | never `DbSet.Update(detached)` |
| PR-B (TaskNotes legacy column) | TaskNote inserts work on upgraded DBs | S3 re-insert depends on it |
| PR-1 merge core | `CanonicalJson.Read/Write/Fingerprint`, `ConflictKeys.ScopeKey`, `ResolutionKind`, `ConflictCandidate` | read-only; no change |
| PR-2 seam | ordinary `SaveChangesAsync` stamping (A2-c) | **no** `MarkSyncApplied`; seam unmodified |
| PR-3 baseline store | — | **not called** (DoR §13.5: baseline not advanced) |
| PR-4 conflict store/schema | `GetAsync`, `MarkResolvedAsync`, triggers, partial index, CHECK | store unmodified; resolver owns every validation `MarkResolvedAsync` explicitly disclaims |
| PR-5 apply session | `SyncBaseFingerprint`, `LocalRowRev`, `LocalWithdrawal`, `EntitySnapshotMapper`, the per-operation-context pattern | session unmodified (unless E-7 extraction); cascade unmodified |
| D4/D9-T4 amendment | AL-PT record shape (null local columns) | resolution of that shape is **B-4** |

---

## 13. Required test matrix

Repo convention: every test names a production mutation that must turn it RED, observed and recorded in
the PR. DoR §17's Z-1..Z-9 are almost entirely **negative** assertions — a resolver that rejects
everything passes all of them. The matrix therefore **leads with positive discriminators**.

**Assertion-design traps the repo has already been bitten by:** (a) use a peer device id, a local
device id and a Base-provenance device id that are all distinct — with shared identities, "stamped
locally" and "kept candidate provenance" produce identical bytes (PR-5 §6); (b) never compute an expected
fingerprint the same way production does — assert the observable consequence (outcome, unchanged row,
unchanged `Rev`, unchanged `ResolvedAtUtc`) instead (the residuals note's degenerate-assertion warning;
PR-5's test M that stopped discriminating); (c) every rejection test needs a positive twin on the same
fixture.

| ID | Test | Mutation that must go RED |
|---|---|---|
| **Z-P1** | S1-CR, no drift: each of KeepLocal / KeepRemote / ManualMerge **is accepted**; row has the expected field values; provenance = local clock + local device; `Rev = before + 1` exactly; record Resolved with kind, `ResultEntityId`, `ResultSnapshotJson`, `ResolvedAt/By` | skip the domain write; call `MarkSyncApplied` on the result (provenance kept ⇒ RED); compare the drift gate against `LocalFingerprint` (⇒ rejects ⇒ RED) |
| **Z-P2** | S1-CR KeepBase: accepted, **no** domain write, `Rev` unchanged, record Resolved | force `Modified` on the entry |
| **Z-P3** | S3 KeepLocal: `N1` re-inserted with `Rev = LocalRowRev + 1`, **and emitted to a third peer whose baseline for `N1` is at `LocalRowRev`** | drop the `Rev` seed (`Rev = 1` ⇒ not emitted ⇒ RED) |
| **Z-P4** | S3 KeepRemote: `N2` inserted, exactly one row in scope; also with a **tombstoned** `N2` | insert without the drift/occupancy check; drop `IsDeleted` from the write |
| **Z-P5** | KeepBase(null) on S3 and on AL-PT: nothing written, `ResultSnapshotJson` null, `ResultEntityId` null, record Resolved, scope unlocked (a new staging in the scope then succeeds) | write an empty/placeholder row; leave Status Unresolved |
| **Z-P6** | ManualMerge brand-new state `D ∉ {A, B, C}` accepted and written | reject any result not equal to a candidate |
| **Z-F1** | round-trip fidelity: right after staging, `Matches(rec.BaseFingerprint, snapshot(live))` is true for S1-CR, S1-PT, S2 (if constructible), and the empty-scope query yields `fp(null)` for S3 / AL-PT — guards the silent "every resolution drifts" failure | include tombstoned rows inconsistently; fingerprint via a different serializer |
| **Z-R1** | replay of the identical request ⇒ `NoOpReplay`, row/`Rev`/`ResolvedAtUtc` unchanged — **including replay after a later local edit to the resolved row** | store the post-stamp fingerprint (⇒ `MismatchedReplay` ⇒ RED); move the drift gate before the replay check (⇒ `LiveStateDrift` ⇒ RED) |
| **Z-R2** | replay with a different kind / result / id ⇒ `MismatchedReplay`, nothing written | treat any request on a Resolved record as `NoOpReplay` |
| **Z-R3** | injected save failure (`FailingSaveDbContext`) ⇒ `Failed`, record Unresolved, row unchanged; retry via a new call ⇒ `Applied`, `Rev` +1 once | reuse the failed context for the retry |
| **Z-D1** | drift (ordinary local edit between staging and resolve) ⇒ `LiveStateDrift` for **all four** kinds incl. KeepBase; nothing written; record Unresolved | skip the drift gate |
| **Z-D2** | drift on an empty-Base scope: a note created for the task (S3) ⇒ `LiveStateDrift` | treat "no row expected" as "don't check" |
| **Z-N1** | AutoLww and AutoTombstone records ⇒ `NotResolvable`, even for a request equal to the stored result; request carrying an auto kind ⇒ `InvalidRequest` | put the replay check before the auto check |
| **Z-N2** | unknown `ConflictId` ⇒ `UnknownConflict` | — |
| **Z-N3** | ManualMerge: missing id ⇒ `InvalidRequest`; id collision ⇒ `ResultIdInUse`; wrong `EntityType`; `MaTask ≠ ConstraintValue` ⇒ `ResultOutOfScope`; non-canonical snapshot ⇒ §5.5 rejection | accept any id / any scope |
| **Z-PT1** | Residual 12: S1-PT and AL-PT KeepRemote ⇒ rejected per **B-2**, no row, record Unresolved; S1-PT KeepLocal with a live parent ⇒ accepted | remove the result parent check (⇒ live child under a tombstoned parent ⇒ RED) |
| **Z-PT2** | parent tombstoned **after** staging (S3; task tombstoned by a later sync) ⇒ KeepLocal/KeepRemote rejected per B-2; KeepBase(null) accepted | parent check only for `StructuralReason == ParentTombstoned` records |
| **Z-PT3** | AL-PT ManualMerge reparented to a live parent ⇒ created; to a tombstoned or missing parent ⇒ rejected | — |
| **Z-E1** | all 24 evidence columns byte-identical before/after every kind, incl. `LocalWithdrawal = HardDeleted` after S3 KeepLocal | write `LocalWithdrawal` (trigger ABORT ⇒ `Failed` ⇒ RED) |
| **Z-T1** | atomicity: fail the save ⇒ neither the domain row nor the record changed (D8-E); context disposed | two saves with a commit between; save the record first |
| **Z-M1** | no `MarkSyncApplied` anywhere in the resolver (source scan, same idiom as the M5 audit fence) — optional, cheap | add one call |
| **Z-C1** (characterization) | F-1: edit a held row, then attempt all four kinds ⇒ all `LiveStateDrift`; a later sync op into the scope ⇒ `ScopeHasUnresolvedConflict` | — (documents frozen behaviour) |
| **Z-C2** (characterization) | F-2 recipe (§11.3) | — |
| **Z-C3** (characterization) | W-3: KeepBase, then re-offer the peer's unchanged row ⇒ applied as a one-sided change | — |

The existing audit fence (`SyncApplyAuditFenceTests`) must stay green unchanged: PR-6 adds no delete.

---

## 14. Scope fences — NOT in PR-6

- Everything in DoR §19 (network, OPEN-2 propagation, supersede/reopen/rebase, resurrection, UI, M2.2
  gate, conflict purge, schema changes on domain tables).
- **No drift gate on the sync-apply path** (§10.2).
- **No second physical-delete site.** Any shape that seems to need a withdrawal (S2 KeepRemote into a
  scope occupied by `N0`) is a typed `ScopeOccupied` rejection, never a widening of M5.
- **No change to PR-5's cascade** or scope-lock behaviour (F-2 is reported, not fixed).
- **No change to `SyncStamper`, `AppDbContext`, `SyncConflictRecordStore`, `SyncBaseSnapshotStore`**, the
  merge core, or the schema/triggers.
- **No baseline write** after resolution (DoR §13.5).
- **No `MarkSyncApplied`** in the resolver (A2-c).
- **No force / rebase / abandon / auto-resolution** as an answer to F-1 or F-2.
- **No resolution UI or batch API**; one conflict per call.
- The resolver must not run while a UI `HocKy` graph is loaded — DoR §15 applies to it exactly as to the
  apply session (`LuuHocKyAsync` would revert a resolved row). Satisfied trivially in M2.1.

---

## 15. Blockers and owner decisions

### 15.1 Blocking — implementation cannot be authorised until these are answered

| # | Question | Options | Engineering recommendation |
|---|---|---|---|
| **B-1** | PR-6 context lifetime (§8.1). The Option A amendment is PR-5-only by its own text. | (a) extend Option A: one `AppDbContext` per `ResolveAsync`, disposed on every path, retry = new call; (b) caller-supplied context, disposal the caller's discipline | (a) — makes the post-stamp failure state (§8.1) unreachable by construction, same rationale as Option A |
| **B-2** | Typed outcome of the result parent check at resolution time (§11.1). Includes Residual 12. Sub-question: is KeepBase exempt when it writes nothing? | (a) reject: `ResultParentTombstoned` / `ResultParentMissing`, record stays Unresolved; (b) for FK-only children, materialise the requested result as a tombstone (mirrors A2-a but changes the requested result); sub-question: (i) KeepBase-no-write exempt, (ii) KeepBase also rejected | (a) + (i): (a) fails closed without changing what the human asked for; (i) because KeepBase creates no new orphan and without it the test-M shape is resolvable only by ManualMerge |
| **B-3** | ManualMerge result with `IsDeleted = true` (§11.4) | (a) reject in v1; (b) allow for leaf types (TaskNote) only; (c) allow with cascade — needs a provenance rule compatible with both A2-b and A2-c | (a) or (b); (c) is new design the DoR never covered |
| **B-4** | KeepLocal on an AL-PT record (no local candidate; amendment silent on resolution) | (a) `Rejected(KindNotApplicable)`; (b) treat as a null result (same live state as KeepBase(null), since AL-PT always has Base = null — §2.1) | (a) — D7-G legitimises a null result only for KeepBase, and the amendment says absence is not a candidate; low stakes either way (only the recorded `ResolutionKind` differs) |

### 15.2 Non-blocking engineering decisions PR-6 records (owner may override)

| # | Decision | Status |
|---|---|---|
| E-1 | `ResultSnapshotJson`/`ResultFingerprint` = canonical of the selected/requested result, pre-stamp (§9.1) | recorded |
| E-2 | Seed `Rev = LocalRowRev` for **any** re-insert of `LocalEntityId`, not only KeepLocal (§8.3) | recorded |
| E-3 | For entity-scoped (Structural) records, `ManualResultEntityId` must equal `EntityId`; DoR §13.3's "fresh Guid" is kept only for constraint scopes. A fresh Guid on an entity scope would leave the scope's row at Base and create a second entity — not a resolution of that scope | **ack requested** — narrows a written DoR rule |
| E-4 | Drift scope = physical rows incl. tombstones, fingerprinted with the staging functions (§10.1) | recorded |
| E-5 | KeepBase with Base present writes no domain row (no forced `Modified`) — forcing it would bump `Rev` and emit Base with local provenance, which the peer's own lock rejects anyway (§13.6) and which does not defend against W-3 | recorded |
| E-6 | Classification order of §7.2 (auto before replay before drift) | recorded |
| E-7 | Parent-rule table and loaders: extract from `SyncApplySession` into an internal helper in a separate, behaviour-preserving first commit (guarded by the existing apply-session suite; `InspectParentAsync` is HIGH, 1 direct caller) **or** duplicate them in the resolver. Extraction avoids two copies of the §12.2 table drifting apart | recorded; owner may prefer duplication to keep `SyncApplySession.cs` untouched |
| E-8 | S2 KeepRemote / ManualMerge with an Id ≠ Base Id ⇒ `Rejected(ScopeOccupied)` (typed, pre-save) | recorded |
| E-9 | Zero `MarkSyncApplied` calls in the resolver; optional source-scan fence (Z-M1) | recorded |

### 15.3 Reported for awareness — not PR-6's to fix, does not gate PR-6

| # | Item | Severity | Label |
|---|---|---|---|
| W-1 | F-1: a drifted record is permanently unresolvable and its scope permanently locked for inbound sync (§10.3). Reachable by any local UI edit once M2.2 exists | **High** (liveness, M2.2) | measured leg + frozen text |
| W-2 | F-2: `CascadeTombstoneAsync` writes into Unresolved scopes, breaking D9-T1 and producing W-1's end state (§11.3). D9-T1 vs D9-T4 collision needs an owner ruling eventually | Medium | INFERENCE + recipe |
| W-3 | A resolution is not durable against the peer's still-live competing state; KeepBase is overridden by the next pull (§9.3). Sharper single-device form of DoR §13.6 / OPEN-2; T2.5's "result valid after resolution" holds only until the next inbound sync | Medium | INFERENCE |
| W-4 | S3 staging can hard-delete a live local `N1` in favour of a remote candidate that is already a tombstone (`ConstraintMerge.cs:41-45` checks only the local side). Physically consistent with the unfiltered UNIQUE index, but worth a PR-1/PR-5 look | Low | FACT (code path) + INFERENCE (reachable) |
| W-5 | GitNexus: canonical `Smart-Study` stale at `723c5e2`; this pass added a third, worktree-derived entry (§1.2) | Medium (tooling) | FACT |
| W-6 | Stale `docs/specs/T2.3-T2.4-D1-D9-Decision-Record.md` still untracked in the owner's tree (DoR housekeeping item) | Low | FACT |

---

## 16. Strengths · Risks · Follow-ups · Final notes

**Strengths.** PR-4 and PR-5 left PR-6 an unusually small job: every primitive in DoR §13.2 exists and
is database-enforced (terminal status, immutable evidence, one-Unresolved-per-scope, local-candidate
CHECK), so an orchestration bug in the resolver fails closed at `SaveChanges` rather than corrupting
evidence. The resolver can be purely additive — it calls the CRITICAL seams and modifies none of them.
A2-c removes the hardest part of PR-5 (provenance preservation) from PR-6 entirely: no marking, no R1
hazard on the write path, no R2 Case B.

**Risks / watchouts.**

| Risk | Severity | Where |
|---|---|---|
| A resolver that rejects everything passes DoR §17's Z tests | High (test design) | §13 — lead with Z-P*, Z-F1, Z-R1 |
| Round-trip infidelity between staging's `BaseFingerprint` and the resolver's live fingerprint ⇒ every resolution silently rejected as drift | High | §10.1, Z-F1 |
| Storing the post-stamp result ⇒ every replay `MismatchedReplay` (violates D7-C) | Medium | §9.1, E-1 |
| Re-inserting `N1` at `Rev = 1` ⇒ never emitted to peers holding older baselines | Medium | §8.3, E-2 |
| F-1 / F-2 / W-3 are properties of the frozen design, not PR-6 bugs; a PR-6 author may be tempted to "fix" them | Medium | §14 fences |

**Suggested follow-ups (non-blocking).** Prune worktree-derived GitNexus entries and re-analyze the
canonical path (W-5). Open an owner ticket for W-1/W-2 before M2.2 planning, with Z-C1/Z-C2 as its
measured baseline. Retire the stale Decision Record copy (W-6). Append the R2 measurement to the
residuals note as the dated amendment PR-5 §11 asked for, now that the note is tracked.

**Final notes.** The design is implementation-ready except for four owner decisions, each of which is
small and each of which the frozen text genuinely leaves open: context lifetime (a governance gap left by
Option A's self-scoping), the resolution-time parent-check outcome (§12.2 was written for apply time),
ManualMerge tombstones (never contemplated), and KeepLocal on the amendment's new record shape. The more
important long-term message is §15.3: under D8-H + D7-D + D9-T6 as frozen, a conflict whose scope drifts
can never be resolved, and a KeepBase does not survive the peer's next offer. PR-6 should implement the
frozen rules faithfully and pin those properties with characterization tests, not paper over them.

---

## Decisions made (ADR-style, engineering only)

| Decision | Why it had to be made | What it is for | Experience it draws on |
|---|---|---|---|
| Treat "apply-time drift gate" as the resolution-time gate only | `ConflictStaging.cs:168-172` states the sync path has no gate and that adding one would invent policy | keeps PR-6 from adding semantics the record does not contain | read the code's own boundary statements before interpreting a brief |
| Generalise Residual 12 into a result parent check for all kinds, and escalate its *outcome* | the residual names one kind, but DoR §13.2 already requires the check on every result, and §12.2's outcomes don't apply at resolution time | stops PR-6 from fixing one instance and leaving the others (KeepLocal after a later parent tombstone) | a residual's blast radius depends on the surrounding design (residuals note, "Decisions made") |
| Put context lifetime in the blocking list | Option A is PR-5-only by its own text; implementing without a ruling would silently extend it | governance consistency with the D4/D9-T4 and Option A pattern | an unflagged extension of a ratified rule propagates as if ratified |
| Label F-1 by leg (measured + frozen) and F-2 as inference with a recipe, and write no throwaway test | the task forbids test code; the repo has the exact precedent (PR-2 residuals note → PR-5 measured) | evidence-scoped claims; the owner can see which leg would need measuring | reproduce-before-escalating, adapted when the boundary forbids measurement |
| Lead the test matrix with positive discriminators | DoR §17 Z tests are satisfiable by a resolver that rejects everything | makes the suite able to go red on the failure that matters most | "a pass is not evidence"; PR-5's test M that stopped discriminating |
| Recommend pre-stamp result fingerprints | post-stamp values include `Clock()` and cannot be recomputed by a replay | D7-C replay identity | determinism requirements on stored fingerprints (DoR §5) |
| Register a separate GitNexus alias rather than refresh the canonical entry | the canonical path is the owner's checkout, which must not be modified | real impact numbers without touching the owner's tree | PR-5 §9 (duplicate entries); CLAUDE.md origin/dev workflow |
