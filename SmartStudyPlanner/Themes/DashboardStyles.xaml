<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <!--
      DASHBOARD REDESIGN — component styles
      =====================================
      Every brush is a {DynamicResource} into the active theme (LightTheme /
      DarkTheme), so the SAME page renders correctly in both modes when
      ThemeManager.ToggleTheme() swaps the dictionary. Subject + severity
      colours come from SubjectPalette.xaml.

      Merge AFTER CommonStyles.xaml.
    -->

    <!-- ─────────── surfaces ─────────── -->
    <Style x:Key="DashCard" TargetType="Border">
        <Setter Property="Background" Value="{DynamicResource CardBackground}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource BorderColor}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="CornerRadius" Value="12"/>
    </Style>

    <Style x:Key="DashPanel" TargetType="Border" BasedOn="{StaticResource DashCard}">
        <Setter Property="Padding" Value="18"/>
    </Style>

    <!-- ─────────── section header ─────────── -->
    <!-- Use together: a 18×3 colour tick + title + optional sub. The tick
         colour is set inline per section (DangerColor, SuccessColor, …). -->
    <Style x:Key="DashSectionTick" TargetType="Border">
        <Setter Property="Width" Value="18"/>
        <Setter Property="Height" Value="3"/>
        <Setter Property="CornerRadius" Value="2"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="Margin" Value="0,0,10,0"/>
    </Style>

    <Style x:Key="DashSectionTitle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="FontWeight" Value="Bold"/>
        <Setter Property="Foreground" Value="{DynamicResource PrimaryText}"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <!-- letter-spacing emulated via per-run text in markup (XAML has no
             text-transform / letter-spacing); titles are authored UPPERCASE. -->
    </Style>

    <Style x:Key="DashSectionSub" TargetType="TextBlock">
        <Setter Property="FontSize" Value="11.5"/>
        <Setter Property="Foreground" Value="{DynamicResource SecondaryText}"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="Margin" Value="10,0,0,0"/>
    </Style>

    <!-- ─────────── KPI strip ─────────── -->
    <Style x:Key="DashKpiLabel" TargetType="TextBlock">
        <Setter Property="FontSize" Value="10.5"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Foreground" Value="{DynamicResource SecondaryText}"/>
    </Style>

    <Style x:Key="DashKpiValue" TargetType="TextBlock">
        <Setter Property="FontSize" Value="27"/>
        <Setter Property="FontWeight" Value="Bold"/>
        <Setter Property="Foreground" Value="{DynamicResource PrimaryText}"/>
        <Setter Property="Margin" Value="0,3,0,0"/>
    </Style>

    <!-- ─────────── pills ("Mức độ") ─────────── -->
    <Style x:Key="DashPill" TargetType="Border">
        <Setter Property="CornerRadius" Value="999"/>
        <Setter Property="Padding" Value="9,3"/>
        <Setter Property="HorizontalAlignment" Value="Left"/>
    </Style>

    <!-- ─────────── buttons ─────────── -->
    <Style x:Key="DashSolidButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
        <Setter Property="Background" Value="{DynamicResource AccentColor}"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="FontSize" Value="11"/>
        <Setter Property="Padding" Value="11,6"/>
    </Style>

    <Style x:Key="DashGhostButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="{DynamicResource SecondaryText}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource BorderColor}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="FontSize" Value="11"/>
        <Setter Property="Padding" Value="11,6"/>
    </Style>

    <!-- ─────────── meters / bars ─────────── -->
    <Style x:Key="DashMeterTrack" TargetType="Border">
        <Setter Property="Background" Value="{DynamicResource BorderColor}"/>
        <Setter Property="CornerRadius" Value="3"/>
        <Setter Property="Height" Value="4"/>
    </Style>

    <!-- ─────────── schedule row ─────────── -->
    <Style x:Key="DashScheduleRow" TargetType="Border">
        <Setter Property="Background" Value="{DynamicResource ChartBackground}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource BorderColor}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="CornerRadius" Value="9"/>
        <Setter Property="Padding" Value="12,9"/>
        <Setter Property="Margin" Value="0,0,0,8"/>
    </Style>

    <!-- ─────────── adaptation row ─────────── -->
    <Style x:Key="DashAdaptationRow" TargetType="Border" BasedOn="{StaticResource DashCard}">
        <Setter Property="Padding" Value="14,12"/>
        <Setter Property="Margin" Value="0,0,0,10"/>
        <Setter Property="BorderThickness" Value="3,1,1,1"/>
        <Setter Property="BorderBrush" Value="{DynamicResource AccentColor}"/>
    </Style>

    <!-- ─────────── DataGrid (hero deadline table) ─────────── -->
    <Style x:Key="DashDataGridHeader" TargetType="DataGridColumnHeader">
        <Setter Property="Background" Value="{DynamicResource CardBackground}"/>
        <Setter Property="Foreground" Value="{DynamicResource SecondaryText}"/>
        <Setter Property="FontSize" Value="10.5"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="BorderBrush" Value="{DynamicResource BorderColor}"/>
        <Setter Property="BorderThickness" Value="0,0,0,1"/>
        <Setter Property="Padding" Value="14,11"/>
        <Setter Property="HorizontalContentAlignment" Value="Left"/>
    </Style>

    <Style x:Key="DashDataGridRow" TargetType="DataGridRow">
        <Setter Property="Background" Value="{DynamicResource CardBackground}"/>
        <Setter Property="Foreground" Value="{DynamicResource PrimaryText}"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{DynamicResource SurfaceHover}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="DashDataGridCell" TargetType="DataGridCell">
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Padding" Value="14,12"/>
        <Setter Property="FontSize" Value="13"/>
        <Setter Property="Foreground" Value="{DynamicResource PrimaryText}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="DataGridCell">
                    <Border Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
                        <ContentPresenter VerticalAlignment="Center"/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsSelected" Value="True">
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="Foreground" Value="{DynamicResource PrimaryText}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="DashDataGrid" TargetType="DataGrid">
        <Setter Property="AutoGenerateColumns" Value="False"/>
        <Setter Property="IsReadOnly" Value="True"/>
        <Setter Property="CanUserAddRows" Value="False"/>
        <Setter Property="CanUserResizeRows" Value="False"/>
        <Setter Property="HeadersVisibility" Value="Column"/>
        <Setter Property="SelectionMode" Value="Single"/>
        <Setter Property="Background" Value="{DynamicResource CardBackground}"/>
        <Setter Property="Foreground" Value="{DynamicResource PrimaryText}"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="RowBackground" Value="{DynamicResource CardBackground}"/>
        <Setter Property="GridLinesVisibility" Value="Horizontal"/>
        <Setter Property="HorizontalGridLinesBrush" Value="{DynamicResource BorderColor}"/>
        <Setter Property="RowHeight" Value="52"/>
        <Setter Property="ColumnHeaderStyle" Value="{StaticResource DashDataGridHeader}"/>
        <Setter Property="RowStyle" Value="{StaticResource DashDataGridRow}"/>
        <Setter Property="CellStyle" Value="{StaticResource DashDataGridCell}"/>
    </Style>
</ResourceDictionary>
