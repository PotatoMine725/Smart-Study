<Window x:Class="SmartStudyPlanner.Views.WorkloadBalancerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Workload Balancer - Trợ lý Lên lịch" Height="650" Width="500" 
        WindowStartupLocation="CenterOwner" Background="#F4F6F7">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="⚖️ TỰ ĐỘNG CÂN BẰNG TẢI" FontWeight="Bold" FontSize="20" Foreground="#8E44AD" HorizontalAlignment="Center" Margin="0,0,0,20"/>

        <Border Grid.Row="1" Background="White" CornerRadius="8" Padding="15" Margin="0,0,0,20">
            <StackPanel>
                <TextBlock Text="Năng lượng của bạn hôm nay:" FontWeight="Bold" Foreground="#34495E" Margin="0,0,0,10"/>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,15">
                    <TextBlock Text="{Binding CapacityHours}" FontSize="24" FontWeight="Bold" Foreground="#2980B9" Margin="0,0,5,0"/>
                    <TextBlock Text="Giờ / Ngày" FontSize="16" Foreground="Gray" VerticalAlignment="Bottom" Margin="0,0,0,3"/>
                </StackPanel>

                <Slider Minimum="1" Maximum="8" Value="{Binding CapacityHours}" TickFrequency="1" IsSnapToTickEnabled="True" Margin="0,0,0,15"/>

                <Button Content="XẾP LỊCH LẠI" Command="{Binding GenerateScheduleCommand}" Height="35" Background="#8E44AD" Foreground="White" FontWeight="Bold" Cursor="Hand"/>
            </StackPanel>
        </Border>

        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding Schedule}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Margin="0,0,0,15">
                            <TextBlock Text="{Binding HeaderText}" FontWeight="Bold" FontSize="16" Foreground="#27AE60" Margin="0,0,0,5"/>
                            <ItemsControl ItemsSource="{Binding Tasks}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Background="White" CornerRadius="5" Padding="10" Margin="0,0,0,5">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>
                                                <StackPanel Grid.Column="0">
                                                    <TextBlock Text="{Binding TenTask}" FontWeight="Bold"/>
                                                    <TextBlock Text="{Binding TenMon}" FontSize="12" Foreground="Gray"/>
                                                </StackPanel>
                                                <TextBlock Grid.Column="1" Text="{Binding ThoiGianHienThi}" FontWeight="Bold" Foreground="#E67E22" VerticalAlignment="Center"/>
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</Window>